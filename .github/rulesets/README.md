# GitHub rulesets

This directory is reserved for exported or intended GitHub ruleset definitions.

The source of truth for the desired settings is `.github/repository-rules.md` until the actual GitHub rulesets are
created and exported. Ruleset files in this directory are documentation unless they are applied through the GitHub API
by an owner with administrative access.
